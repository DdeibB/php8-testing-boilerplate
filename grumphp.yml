grumphp:
    tasks:
        phpunit:
        phpmd:
            report_format: text
            ruleset: [ 'cleancode', 'codesize', 'naming' ]
            triggered_by: [ 'php' ]
        phpcs:
            standard: PSR12
